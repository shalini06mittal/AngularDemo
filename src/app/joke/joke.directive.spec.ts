import { JokeDirective } from './joke.directive';

describe('JokeDirective', () => {
  it('should create an instance', () => {
    const directive = new JokeDirective();
    expect(directive).toBeTruthy();
  });
});
